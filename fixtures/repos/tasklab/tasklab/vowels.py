"""slib-runnable: stdlib `re` only."""


import re


def count_vowels(s: str) -> int:
    """Return the number of vowels (aeiou, case-insensitive) in s."""
    # CONCORD_TASK_BEGIN
    return 0
    # CONCORD_TASK_END
